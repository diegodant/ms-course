spring.application.name=hr-user
spring.cloud.config.uri=http://localhost:8888

spring.profiles.active=dev

#permite que actuator possa atualizações durante o runtime
management.endpoints.web.exposure.include=*